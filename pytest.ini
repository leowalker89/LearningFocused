[pytest]
# Ensure `import src...` works during test collection/runs.
# This repo is not installed as a package during local dev runs; we rely on
# adding the project root to the Python path for tests.
pythonpath = .

# Keep output readable; developers can override on the CLI.
addopts = -ra

testpaths =
    src/tests


